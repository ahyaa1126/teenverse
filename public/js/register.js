const form=document.getElementById('registerForm'),statusEl=document.getElementById('status');
form.addEventListener('submit',async e=>{e.preventDefault();statusEl.textContent='Creating account…';statusEl.className='status';try{const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username:username.value.trim(),password:password.value,age:Number(age.value),gender:gender.value,country:country.value.trim()||'Not set'})});const d=await r.json();if(!r.ok)throw new Error(d.message||'Registration failed');statusEl.textContent='Account created. Redirecting…';statusEl.className='status success';setTimeout(()=>location.href='login.html',900)}catch(err){statusEl.textContent=err.message;statusEl.className='status error'}});
